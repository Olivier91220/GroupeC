SELECT * FROM `intervention` 
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON
INNER JOIN			ON